import { EmptyState } from "@/components/common/empty-state";
export const metadata = { title: "Workspace" };
export default function DashboardPage() { return <EmptyState title="Your workspace is ready" description="Platform modules will be introduced as they are delivered. This workspace contains no business data yet." />; }
